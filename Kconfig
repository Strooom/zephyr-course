rsource "lib/breathing_led/Kconfig"
source "Kconfig.zephyr"

menu "WS2812 Strip Configuration"

config SAMPLE_LED_UPDATE_DELAY
	int "Delay between LED updates in ms"
	default 50
	help
	  Delay between LED updates in ms.
endmenu
